export interface Project {
    title: string,
    subtitle: string,
    headerClass: string,
    avatarSrc: string,
    desc: string,
    designUrl: string | null,
    gitUrl: string | null,
    liveUrl: string | null,
}